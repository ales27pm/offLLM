name: iOS Build → xcresult JSON

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  ios:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (for RN codegen)
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install JS deps
        run: npm ci

      - name: Setup Ruby & CocoaPods
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"

      - name: Install CocoaPods & xcpretty
        shell: bash
        run: |
          set -euo pipefail
          gem install cocoapods -N
          gem install xcpretty -N
          pod --version
          ruby --version

      # 🔧 Ensure the Xcode project exists before running `pod install`
      - name: Ensure iOS project exists (.xcodeproj)
        shell: bash
        run: |
          set -euo pipefail
          echo "Listing ios/"
          ls -la ios || true

          # If you use XcodeGen, generate the project first
          if [ -f "ios/MyOfflineLLMApp/project.yml" ]; then
            echo "→ Found ios/MyOfflineLLMApp/project.yml — generating Xcode project with XcodeGen"
            brew install xcodegen || true
            (cd ios/MyOfflineLLMApp && xcodegen generate)
          # Or Tuist
          elif [ -f "ios/Project.swift" ] || [ -d "ios/Tuist" ]; then
            echo "→ Found Tuist configuration — generating Xcode project with Tuist"
            curl -Ls https://install.tuist.io | bash
            (cd ios && ~/.tuist/bin/tuist generate)
          fi

          echo "After generation, look for .xcodeproj:"
          find ios -maxdepth 3 -name "*.xcodeproj" -print

          if ! find ios -maxdepth 3 -name "*.xcodeproj" | grep -q . ; then
            echo "::error::No .xcodeproj found near Podfile. Ensure project generation (XcodeGen/Tuist) occurs before 'pod install'."
            exit 1
          fi

      - name: Install CocoaPods (after project exists)
        shell: bash
        run: |
          set -euo pipefail
          cd ios
          pod repo update
          pod install --repo-update
          cd -
          # Show where the workspace is
          find ios -maxdepth 3 -name "*.xcworkspace" -print

      - name: Xcode & Simulator diagnostics
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild -version
          xcrun simctl list devices | head -n 50

      - name: Build (Simulator) and export .xcresult
        shell: bash
        env:
          SCHEME: MyOfflineLLMApp
          WORKSPACE: ios/MyOfflineLLMApp.xcworkspace
          CONFIGURATION: Debug
          SDK: iphonesimulator
          DESTINATION: "platform=iOS Simulator,name=iPhone 15"
          RESULT_BUNDLE: ${{ runner.temp }}/MyOfflineLLMApp.xcresult
          RESULT_JSON: ${{ runner.temp }}/MyOfflineLLMApp.xcresult.json
        run: |
          set -euo pipefail

          echo "→ xcodebuild (workspace: $WORKSPACE, scheme: $SCHEME)"
          xcodebuild \
            -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration "$CONFIGURATION" \
            -sdk "$SDK" \
            -destination "$DESTINATION" \
            -resultBundlePath "$RESULT_BUNDLE" \
            -showBuildTimingSummary \
            build | xcpretty || true

          echo "→ Convert .xcresult to JSON with xcresulttool"
          /usr/bin/xcrun xcresulttool get \
            --format json \
            --path "$RESULT_BUNDLE" \
            > "$RESULT_JSON"

          echo "Result bundle: $RESULT_BUNDLE"
          echo "Result JSON:   $RESULT_JSON"

      - name: Upload .xcresult bundle
        uses: actions/upload-artifact@v4
        with:
          name: MyOfflineLLMApp-xcresult
          path: ${{ runner.temp }}/MyOfflineLLMApp.xcresult

      - name: Upload xcresult JSON
        uses: actions/upload-artifact@v4
        with:
          name: MyOfflineLLMApp-xcresult.json
          path: ${{ runner.temp }}/MyOfflineLLMApp.xcresult.json
